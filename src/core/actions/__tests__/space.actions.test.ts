import { describe, it } from "vitest";

describe("space.actions (Server Actions)", () => {
  it.todo("blockResourceAction — coberto via useSpacesPage tests");
  it.todo("unblockResourceAction — coberto via useSpacesPage tests");
});
