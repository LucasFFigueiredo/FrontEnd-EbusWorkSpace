import { describe, it } from "vitest";

describe("user.actions (Server Actions)", () => {
  it.todo("updateUserDepartmentAction — coberto via useSelectDepartmentPage tests");
  it.todo("requestAccessAction — coberto via useProfilePage tests");
  it.todo("updateUserRoleAction — coberto via useAdminPage tests");
  it.todo("approveAccessRequestAction — coberto via useAdminPage tests");
});
