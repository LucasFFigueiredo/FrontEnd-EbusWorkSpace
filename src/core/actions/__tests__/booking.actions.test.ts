import { describe, it } from "vitest";

describe("booking.actions (Server Actions)", () => {
  it.todo("createReservationAction — coberto via E2E / testes de integração");
  it.todo("cancelBookingAction — coberto via useMyBookingsPage e useSpacesPage tests");
  it.todo("checkInAction — coberto via useCheckinPage tests");
  it.todo("requestExtensionAction — coberto via useMyBookingsPage tests");
  it.todo("approveReservationAction — coberto via useSpacesPage tests");
});
