/** @type {import('postcss').ProcessOptions} */
const config = {
  plugins: {
    // Plugin oficial do Tailwind CSS v4 para ambientes PostCSS (não-Vite)
    "@tailwindcss/postcss": {},
  },
};

export default config;
